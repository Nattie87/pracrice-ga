window.addEventListener('DOMContentLoaded', () => {
  const button = document.querySelector('button');
  button.addEventListener('click', () => {
    console.log('You clicked me');
  });

});










// const button = document.querySelector('button');
// ///Returns the first Element within the document
// button.addEventListener('click', () => { //arrow function short syntax
//   console.log('You clicked me');
// });
//
// //can anyone tell me in 2 mins of googling 'when the browser has loaded all of the HTML and build the DOM memory it fires up and event to the window... what is that called? '
